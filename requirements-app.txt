# ALEXIS desktop app - runtime dependencies (pinned to the verified build env:
# Windows Python 3.11.9). The legacy requirements.txt was an incomplete IPython
# dump; this is the real set the app + classifiers + viz generation need.
#
# Install:  py -3.11 -m pip install -r requirements-app.txt

# --- ML / data (the sklearn pin MUST match the pickled models: 1.6.1) ---
scikit-learn==1.6.1
scipy==1.15.3
numpy==2.2.5
pandas==2.2.3
pyarrow==22.0.0

# --- desktop app (GUI + local server) ---
fastapi==0.138.0
uvicorn==0.49.0
sse-starlette==3.4.5
pywebview==6.2.1
httpx==0.28.1

# --- data ingest / parsing ---
beautifulsoup4==4.12.2
lxml==6.1.1
openpyxl==3.1.5
ijson==3.2.3
tqdm==4.67.1

# --- packaging (build-time only) ---
pyinstaller==6.20.0

# --- scraping jobs only (ChiCTR/ANZCTR); install separately when needed:
#       py -3.11 -m pip install playwright && py -3.11 -m playwright install chromium
# playwright==1.48.0
